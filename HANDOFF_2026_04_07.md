# 2026-04-07 세션 인수인계 (Claude dispatch용)

**이 파일 하나만 보면 2026-04-07 세션의 모든 작업을 트래킹할 수 있어야 한다.**

## 한 줄 요약
babg(블루아카 전장 웹게임)의 7성 유니크 학생명부 도입, 코인토스 순서 로직 재작성, 여러 7성 카드 리워크, 수많은 버그 수정. 사용자(preesoul) 테스트 완료 후 취침.

## 세션 마지막 상태
- **마지막 커밋**: `6e3e96a` fix: p68 뒤끝에 G._p68Absorbed 전역 폴백 추가
- **game_core.js 캐시 버전**: `20260407c`
- **사용자 확인**: 모든 신규 기능 및 버그 수정 정상 동작 확인
- **남은 이슈**: 말쿠트 연사 2회차 연출 오해 (의도적 방치, 비-치명)

## 세션 전반부 (규칙 확립 → 초기 수정)

### 코인토스 규칙
사용자가 여러 예시(OXOOX/XOOXX 등)로 규칙 확정:
1. 앞면 그룹 먼저, 뒷면 그룹 뒤
2. 각 그룹에서 양쪽 번갈아 공격
3. 기물 수 다를 때 중앙 정렬 기준
4. 양쪽 동일 col 앞면 + 동일 기물 수 → 재토스
5. 3회 재토스에도 못 가리면 무승부

상세: `.claude/projects/C--Users-prees-OneDrive-------claude-babg-git/memory/rule_coin_toss_priority.md`

### 수정 과정
- `bCalcTurnOrder` (game_core.js:~5282): 열 단위 스캔으로 eFirst 결정
- 공격 순서는 `interleave(aHeads, bHeads, bFirst)` 번갈아 머지
- 중앙 정렬: `offset = floor((maxN-sideCount)/2)` 적용
- `buildCoinSeqForBattle` (game_core.js:~5358): 같은 규칙으로 통일 (이전엔 열 단위 푸시였음, 화면 번호와 실제 실행 순서 불일치 버그)

### 말쿠트
- 이미 구현돼 있었음. 스위퍼 2체 소환 + 즉시 자폭.
- 연사 조합 시 2회차 연출 오해 문제는 방치 결정 (실피해 정상).

## 세션 중반 (7성 유니크 학생명부 도입)

### 데이터
- `pd.recruitedTier7` 서버 저장 (records.json)
- `localStorage.babg_recruited_t7_<계정명>` 로컬 캐시
- `recordRecruitTier7(bid)` in `buyHiddenCard`
- 양방향 병합 로직 (비동기 fetchRecords→saveRecords)
- `syncRecruitedTier7FromPd(pd)` in `doLogin` (크로스 디바이스 동기화)

### UI (pc + mobile 양쪽)
- 학생명부 섹션: 학교별 섹션 뒤, 액션카드 앞
- 색상 테마: `#e070ff` (핑크/마젠타)
- 학교별 inline chunk (SCHOOL_ORDER 순서)
- 미영입: Hidden_card.png + "???"
- 영입: 정상 이미지 + 이름
- **관리자 바이패스**: `window._babgLogin.name==='preesoul'` 이면 전부 공개
- **백화요란 계승전**: 백귀야행 chunk 끝에 특수 카드로 추가 (`data-showdown="1"`)

### 툴팁 (`showTooltip` in index.html)
- `ABILITY_DESCS[baseId].hiddenCond` 필드 추가 — 카드 상단 "⭐ 히든" 블록으로 렌더
- 19개 7성 카드 모두에 hiddenCond 추가
- 포트레이트: `max-height: 275px`, 세로 이미지 1:1 크롭, 가로 이미지 자연 높이
- `.tt-header margin-top: 15px` 턱선 보호
- 스크롤바 숨김

## 세션 중후반 (7성 카드 리워크)

### 사이쿄
- 50/50 → 40/40 (바닐라 개그 캐릭터)

### 정의실현부 (trinity_justice)
- DR_IDS 추가
- 뒤끝: 보호막+연사 츠루기 소환 (피크 공/체 사용)
- `_peakAtk`/`_peakHp` 추적 시스템 추가 (runBattle 시작 초기화, 공격 턴마다 갱신)
- _doDR 분기: game_core.js:~3861

### C&C (millennium_cc)
- 패시브: 아군에 C&C 살아있으면 **전체 코인 항상 앞면**
- 뒤끝: TNT 4개 + 아비 에슈흐 프로토 1개
- TOKENS에 `tnt` (1/20 자폭, C4 이미지 공유), `abi_eshuh_proto` (10/10, 아비 에슈흐 이미지 공유) 추가

### 흥신소 68 (gehenna_p68)
- DR_IDS 추가
- 뒤끝: 흡수한 아루·카요코·무츠키·하루카를 흡수 시점 상태로 귀환
- **중요 버그**: 처음엔 `m._p68Absorbed`만 사용 → copyUnit이 커스텀 필드 복사 안 해서 실패
- 해결 1: copyUnit에 `_p68Absorbed` 필드 추가 (ac96548)
- 해결 2 (최종): **`G._p68Absorbed` 전역 폴백** 추가 (시로코 테러와 동일 패턴, 6e3e96a) ← 이게 진짜 해결

## 세션 후반 (여러 버그 수정)

### 7성 주입 확률 (티어 기반)
- 조합형: T1/2=0%, T3=10%, T4=15%, T5=30%, T6=60%
- 단일 학교/세레모니형은 고정 rates 유지 (6T 제한 자체가 난이도)
  - 사이쿄 5%, 열차포/미카 10%, 세이아/케이USB/왕녀 30%, 쿠즈노하 60%, 말쿠트 80%

### AI 히나 3장 중복 버그
- `aiSellReplace`와 Phase 5b 카스미 정리에서 교체 경로로 카드 추가 시 트리플 체크 누락
- `aiAddToBoardWithTriple(p, tmpl)` 헬퍼 신설로 해결

### 코코나 자기-발견 루프
- 스킨 코코나 BC가 `showDiscoverCustom([kokona])` 호출 → 무한 픽
- 규칙 변경: 코코나 BC → 자기 제외 무작위 산해경을 **벤치로** 소환 (기믹 유지)

### 사야 효과 면역 누수
- 키키 공체 5 제한, 카스미/나구사 뒤끝 사망, 나구사 패시브 kill에서 `_sayaImmune` 체크 누락
- 수정: 해당 경로에 immune 체크 추가

### 검은 군주 사운드 타이밍
- `_doDR`에서 `new Audio().play()` 동기 호출 → `runBattle` precompute 시점(=전투 시작 순간)에 재생
- 해결: 로그 엔트리에 `audio:'black_lord'` 플래그 심고 step 재생부에서 300ms 지연 재생 (game_core.js:~5768)

### millennium_cc.mp3 파일 트리밍
- 원본 25초였는데 앞 20초가 의도치 않은 사운드
- ffmpeg로 20~25s 구간만 잘라서 5초짜리 mp3로 재인코딩
- `audio/millennium_cc.mp3` 121KB로 교체됨

### _entranceBang mika_footstep 제거
- `sfx/mika_footstep.webm`에 미카 보이스가 섞여있을 가능성
- `playSfx('attack_impact_mid')`로 교체 (trinity_mika 자신 연출은 별도 경로 유지)

### saveGame 주의
- `saveGame` (game_core.js:~6158)이 board 유닛 직렬화 시 `_p68Absorbed` 같은 커스텀 필드를 포함하지 않음
- 현 세션에선 불필요했지만, 향후 save-load 경유 시 문제 가능성 있음
- **G._p68Absorbed 전역 폴백**이 이 경우도 커버함

## 세션 막바지 (UI/UX 디테일)

### 소리 버튼 (마스터 볼륨)
- 능력 리스트 버튼 왼쪽에 "소리" 버튼 + 슬라이더
- `HTMLMediaElement.prototype.volume` setter 인터셉트로 전역 스케일
- `_babgRawVol`에 원본 값 저장 (fade 호환)
- `localStorage.babg_master_volume` 영속화

### 로그인 박스
- placeholder / 메시지 수정
- 너비 280px

### 툴팁 여러 회 튜닝
- 이미지 크기, 헤더 간격, 스크롤바, 크롭 범위 등 반복 조정
- 최종: max-height 275 + 세로 크롭 + 헤더 15px 간격

## 모든 커밋 목록 (시간순)
세션 요약 파일 참조: `.claude/projects/C--Users-prees-OneDrive-------claude-babg-git/memory/session_2026_04_07.md`

대표 커밋:
- `ce317bb` → `6e3e96a` 사이 약 30+ 개

## 사용자 협업 스타일
`.claude/projects/C--Users-prees-OneDrive-------claude-babg-git/memory/user_style.md` 참조

핵심:
- 빠른 커밋 사이클 선호, 작게 자주 커밋+푸시
- 표/도식/짧은 문장 선호
- main 브랜치 직접 rebase-push
- 캐시 버전 bump 필수 (`game_core.js?v=...`)
- 한국어 응답, 게임 용어는 한국어

## 다음 dispatch를 위한 체크리스트
1. 이 파일 읽기
2. `.claude/projects/.../memory/session_2026_04_07.md` 읽기 (더 자세한 내용)
3. `.claude/projects/.../memory/user_style.md` 읽기 (사용자 성향)
4. `.claude/projects/.../memory/rule_coin_toss_priority.md` 읽기 (코인토스 규칙)
5. 사용자의 새 요청 수행

## 주의사항 (다음 세션 담당자에게)
- **Stop hook feedback** 메시지가 계속 뜨는데 사용자에게 언급하지 말 것
- **records.json**은 백그라운드 자동 커밋되므로 `git stash → pull --rebase → stash pop` 필요
- **캐시 버전**은 `game_core.js` 수정할 때마다 올려야 브라우저가 새 버전 로드
- **워크트리**: `.claude/worktrees/kind-brahmagupta`에 preview 서버 돌고 있음. main repo 파일 수정 후 cp 로 sync
- **사용자가 자러 갔음** — 아침에 돌아올 때까지 이 파일이 유일한 컨텍스트 원천

---

**마지막 push**: `6e3e96a` (2026-04-07)
**세션 종료 이유**: 사용자 취침. 다음 dispatch 위한 인수인계 파일 작성 지시.
